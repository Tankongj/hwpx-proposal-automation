# Core dependencies
lxml>=4.9.0
pdfplumber>=0.9.0

# Optional: HWPX document handling
python-hwpx>=0.1.0

# Optional: Style configuration files
PyYAML>=6.0

# Optional: Chart generation (for proposal visuals)
matplotlib>=3.7.0
numpy>=1.24.0

# Optional: PPT extraction
python-pptx>=0.6.21

# Optional: COM API for Hancom Office (Windows only)
# pip install pywin32
# pywin32>=306
